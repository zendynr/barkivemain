// This component is not in use anymore and can be deleted.
// The dashboard has been simplified and memories are now exclusively in the "Memories" tab.
export function MemoriesCarousel() {
  return null;
}
